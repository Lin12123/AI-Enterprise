# Extrusion

Status: scaffolded.

Current executor work maps allowlisted FeaturePlan `create_base_plate` operations to a fixed SOLIDWORKS API call sequence. This page does not contain executable runtime code.

Required policy checks:

- profile source is allowlisted;
- extrusion depth is positive and within project limits;
- operation mode is allowlisted.
